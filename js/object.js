"use strict";
// object type declare
const obj = { name: 'rocky', age: 100 };
//type declare arrow function 
const numbers = (num1, num2) => num1 + num2;
const dataNumber = (num1, num2) => {
    console.log(num1 + num2);
};
